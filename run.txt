# INPUT_CSV: header row; part numbers in part_number / sku / stockid (auto) or --sku-column
# OUTPUT_CSV: one row per input — filled only when API stockid exactly matches that part
python noramstore_scraper.py "all skus.csv" results.csv

python noramstore_scraper.py threeSKUs.csv out.csv --verbose

# Optional: throttle requests for large runs
python noramstore_scraper.py "all skus.csv" results.csv --sleep 0.3 --sleep-between-searches 0.5

# Cap API pages scanned per part (testing)
python noramstore_scraper.py threeSKUs.csv out.csv --max-pages 2 --limit 50

# Continue after a stop: same INPUT_CSV and OUTPUT_CSV; appends rows, does not overwrite
python noramstore_scraper.py "all skus.csv" results.csv --resume

# Full catalog: years → makes → models (depth-first), then HTML parse (same CSV as noramstore_scraper)
python noramstore_catalog_scraper.py --skus-file "all skus.csv" --all-products all_products.csv --fitment-products fitment_products.csv
python noramstore_catalog_scraper.py --skus-file "all skus.csv" --resume
# Optional: --html-sleep 0.2  (pause after each product page fetch; API uses --sleep)
# Old flat-index checkpoints are not compatible — delete crawl_checkpoint.json or use a new --checkpoint
